# Local initialization and administrative access.
local   all             all                                     trust
host    all             postgres        all                     trust

# Each test user intentionally exercises a different authentication method.
host    all             auth_trust      all                     trust
host    all             auth_password   all                     password
host    all             auth_scram      all                     scram-sha-256
