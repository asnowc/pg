# Test-only rules. This configuration is intentionally unsuitable for production.
local       all         all                                     trust
host        all         postgres        0.0.0.0/0               trust
host        all         postgres        ::/0                    trust

# Each role intentionally exercises a distinct protocol path.
hostnossl   test_public   auth_trust      0.0.0.0/0               trust
hostnossl   test_public   auth_trust      ::/0                    trust
hostnossl   test_public   auth_password   0.0.0.0/0               password
hostnossl   test_public   auth_password   ::/0                    password
hostnossl   test_public   auth_scram      0.0.0.0/0               scram-sha-256
hostnossl   test_public   auth_scram      ::/0                    scram-sha-256
hostssl     test_public   auth_tls        0.0.0.0/0               trust
hostssl     test_public   auth_tls        ::/0                    trust

host        all         all             0.0.0.0/0               reject
host        all         all             ::/0                    reject
